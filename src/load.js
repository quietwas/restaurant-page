import "./styles.css"

// body display is set to none originally and then set back to block once the js is added so there is no visual stutter
const body = document.querySelector("body")
body.style.display = "block"